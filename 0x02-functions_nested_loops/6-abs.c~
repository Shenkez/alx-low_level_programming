#include "main.h"

/**
 * _abs - check the code
 *k: function parameter
 * Return: Always 0.
 */

int _abs(int k)
{
int abs_val;
if (k > 0)
{
_putchar(k);
}
else
{
abs_val = k * -1;
_putchar(abs_val);
}
return (k);
}
