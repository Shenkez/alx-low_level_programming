#include "main.h"
#include <stdio.h>

/**
 * mul - checks the code.
 * @a: first parameters passed into the function
 * @b: second parameter
 * Return: 0
 */
int mul(int a, int b)
{


	return (a * b);
}
