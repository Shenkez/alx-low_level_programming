#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks the code.
 * @c: parameter poassed
 * Return: 1 if capital and 0 otherwise.
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	{
	return (0);
	}

}
